# Host configuration for multinode setup
# Add these entries to /etc/hosts on both controller and compute nodes

# DevStack Multinode Setup
<controller_ip>   controller
<compute_ip>      compute1

# Optional: Add additional compute nodes
# <compute2_ip>   compute2
# <compute3_ip>   compute3

# To apply: 
# sudo cp hosts-example /etc/hosts
# or append to existing /etc/hosts file
